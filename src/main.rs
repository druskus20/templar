#![allow(dead_code)]
#![feature(drain_filter)]

mod config;
mod templating;
mod utils;

fn main() {
    println!("Hello, world!, \"main()\" is not yet implemented, however, however, there are tests");
}
